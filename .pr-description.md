## Summary

依据内部 PRD《Neo Desktop 开源版重构实现文档》，把内部版 `neo-monorepo` 中**非内部专属、非 daemon 专属**的功能域移植/重构到开源仓库。基底架构不动：保留单包、无 daemon、main 内 agent、Neovate 品牌、husky/lint-staged。

> **重要约束**：无法访问内部 `neo-monorepo` 源码，所有移植域按 PRD 字面规格**从零实现**，纯本机、零 `@alipay/*` / 内网依赖。本 PR 含 **P0–P3** 四个 commit：P0 是 Provider 双模式授权 + 去 CDN 化（核心地基），P1–P3 在其上推进。`dev-workflow` 与 `neo-desktop-mcp` 是 PRD 字面最小实现，非内部版等价物。

按阶段拆成 4 个 commit，每个基于上一 commit 编译通过。

---

## Changes by Stage

### P0 — Provider 双模式授权 + 去 CDN 化 (`656bfac`)

**Provider 双模式开箱即用授权（核心，PRD §5）：**

- `Provider` 类型加 `auth: "inherit" | "api-key"`；contract 放宽 `apiKey`/`baseURL`/`models` 为可选以支持 inherit 无凭据模式。
- `session-manager` ENV 注入加 inherit 分支：不注入 `ANTHROPIC_*` env，SDK 自解析本机 OAuth / `~/.claude` 登录态；`llm-service` 同步处理 inherit。
- 新建 `features/provider/onboarding` 首启向导，双模式（① Claude 订阅 inherit ② API Key + 自定义 Anthropic 兼容端点），挂载于 App 顶层。
- `providers-panel` 表单加 auth 切换，inherit 模式隐藏凭据字段并放宽 validate；`templates` 新增 `anthropic-subscription` inherit 模板。

**品牌 / CDN 去内部化：**

- LICENSE 版权方 Ant UED → Neovate Contributors。
- 9 张静态图 + editor 2 张 logo 全部本地化进 `assets/images/files/`，改用 `new URL(..., import.meta.url)`；`index.html` CSP 移除 `mdn.alipayobjects.com`。
- editor code-server 下载源切回官方 GitHub release；vsix 走 `NEOVATE_EXTENSION_URL` 可配 + GitHub Release 占位。
- `index.ts` appId 按 dev/prod 分离；locales 4 处硬编码 Neovate → `{{APP_NAME}}`，i18n 加 `defaultVariables` 全局注入；README logo 改本地。

### P1 — 基础设施域 (`ca2dc45`)

- **notification**：原生 `Notification` + toast 双通道。新增通用 `main/core/event-bus`（main→renderer 广播基元，填补仓库此前无通用事件流的空白）、preload `onEvent`、renderer `NotificationListener` 复用现有 toast 基元。
- **fs / file 协议**：`neovate-file://` privileged scheme + `protocol.handle` 流式处理器；薄 `fs` service（带绝对路径校验）供 agent 附件 / 配置读取。
- **token-usage（本机统计）**：`TokenUsageEvent` 类型 + `TokenReporter`（按 session 与全局累计，**无外传**），`SessionManager` 在 SDK `result` 事件下发增量，renderer `addSessionTokenUsage` 累加到 `SessionUsage` 累计字段。

### P2 — 工作台核心域 (`8d21db5`)

- **worktree**：`WorktreeService`（simple-git，`.neovate-worktrees/<branch>` 约定），list/create/remove/prune router，renderer `NewWorktreeButton` + dialog，创建后以 worktree 路径为 cwd 起新 session（SessionManager 已 cwd-agnostic）。
- **dev-workflow**：`DevWorkflowService`（stateStore 持久化），`mode=default/plan/dev` 覆盖 SDK `permissionMode`，`draftPrefix` 在每轮用户消息前注入；renderer `DevWorkflowControl`（mode 菜单 + 草稿前置对话框）。PRD 字面最小实现。
- **neo-desktop-mcp**：内置 MCP server 插件，通过 `agents` 贡献注入 SDK `mcpServers`，暴露 `read_file` / `write_file` / `list_directory` / `open_path` / `open_terminal` / `stat_path` 本机工具给 agent。

### P3 — Agent 增强域 (`ae75d30`)

- **agent-plugins 贡献机制补齐**：`PluginManager.onContributionsChanged` 通知钩子、`MainApp.refreshContributions`（重跑 `configContributions`）、`SessionManager.refreshAgentContributions`、`client.agent.refreshContributions()`（插件热插拔后新 session 加载最新贡献）。设计说明见 `docs/designs/agent-contributions.md`（含 harness 多 provider 演进路线）。
- **chat.attachments（重落到 agent 域）**：`SessionManager.send` file-parts 分支放宽 —— image → image block，PDF → document block，文本类 → 内联 text，其他二进制 → `@filename` 路径引用；renderer `message-input` accept 扩宽、`AttachmentPreview` 兼容非图片附件。

---

## Architecture

所有新域遵循开源版既有范式，未引入新架构：
`shared/features/<d>/contract.ts`（zod）→ `main/features/<d>/router.ts`（`os.<d>.router`，`$context<AppContext>`）→ `AppContext` 加 service 字段 + `main/index.ts` 实例化注入 → `main/router.ts` 顶级合并 → renderer `client.<d>.*`。

基底决策（沿用 PRD §3）：

- 进程模型保留「main 内 agent」，**不引入 daemon**
- Agent 抽象暂不引入 harness，仅记录多 provider 演进路线（文档化）
- `neo://` / `neovate-file://` scheme 保留开源版命名

## Compliance

- 新域 `grep -E "@alipay|@antfin|antgroup-inc|alipay\.com|whoami"` ✅ 清零
- P0 去 CDN 化后全仓 `alipayobjects` 0 残留
- 无 `@alipay/*` 依赖引入

## Verification

- ✅ P0：typecheck 绿 / lint 0 errors / 585 tests 全通过 / 全仓 `alipayobjects` 0 残留
- ✅ P1：`bun check` + `bun test:run`（589 passed）全绿
- ✅ P2 / P3：typecheck 0 错误（本次文件）
- ⚠️ 全量 `bun ready` 当前因**外部并发的 `components/ui → packages/ui` 抽取**（半成品态：`tsconfig.web.json` 的 `baseUrl` 被 tsc 拒绝、`@neo/ui` import 解析未配齐）无法通过——**非本 PR 引入**，且未进入本 PR 范围。待该并发工作收尾后 `bun ready` 应一并绿。
- ⏳ `bun build:mac` 打包冒烟待并发 UI 工作稳定后跑

## Test plan

- [ ] 首启无 Provider：onboarding 向导弹出，① inherit 首启发消息（OAuth 引导）② 填 API key + 自定义 baseURL 发消息（env 注入正确）
- [ ] providers-panel 切 inherit/api-key 模式表单字段正确显隐
- [ ] 9 张静态图 + editor logo 走本地加载，无 `mdn.alipayobjects.com` 请求
- [ ] `bun dev` 首启：notification `client.notification.show` 触发 toast + 系统通知
- [ ] `neovate-file://<abs-path>` 在 renderer 加载图片正常
- [ ] 跑一轮 agent：`client.tokenUsage.getSessionUsage` 累计正确、无外发请求
- [ ] 侧栏「New Worktree」→ 新建 → 在 worktree 内起新 session → 删除 worktree
- [ ] dev-workflow 切 plan/dev 模式生效；draftPrefix 出现在 session 首条 prompt
- [ ] session 内 agent 可调用 `neo-desktop` MCP 的 `read_file` 等工具
- [ ] `client.agent.refreshContributions()` 触发后新 session 加载最新 MCP server
- [ ] 聊天输入附加 PDF / 文本 / 图片，session 收到对应 block
